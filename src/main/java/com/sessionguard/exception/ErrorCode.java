package com.sessionguard.exception;

public enum ErrorCode {
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    SESSION_NOT_FOUND,
    USER_ALREADY_EXISTS,
    INVALID_CREDENTIALS,
    VALIDATION_FAILED,
    INTERNAL_ERROR
}